<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>ai.pdlc</groupId>
    <artifactId>pdlc-pilot</artifactId>
    <version>0.1.0-SNAPSHOT</version>
  </parent>
  <artifactId>agents</artifactId>
  <name>agents</name>
  <description>Embabel-backed grill + PO reasoning agents, plus the Temporal worker hosting AgentActivities on queue "reasoning".</description>

  <dependencies>
    <dependency>
      <groupId>ai.pdlc</groupId>
      <artifactId>core</artifactId>
    </dependency>
    <dependency>
      <groupId>ai.pdlc</groupId>
      <artifactId>adapters</artifactId>
    </dependency>

    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-web</artifactId>
    </dependency>
    <!-- Only for the plain-JdbcTemplate `runs` table insert; no spring-data-jdbc here (plan step 6). -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-jdbc</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-opentelemetry</artifactId>
    </dependency>

    <!-- Embabel agent platform (provides the injectable Ai bean + AgentPlatform). -->
    <dependency>
      <groupId>com.embabel.agent</groupId>
      <artifactId>embabel-agent-starter</artifactId>
    </dependency>
    <!-- Custom OpenAI-compatible endpoint support (OPENAI_CUSTOM_BASE_URL/API_KEY/MODELS):
         the documented mechanism for pointing Embabel at stub-llm. Verified from the v1.5.1 jar. -->
    <dependency>
      <groupId>com.embabel.agent</groupId>
      <artifactId>embabel-agent-openai-custom-autoconfigure</artifactId>
    </dependency>

    <!-- Prompt/template rendering for LLM prompts and release-pack markdown. -->
    <dependency>
      <groupId>com.github.spullara.mustache.java</groupId>
      <artifactId>compiler</artifactId>
    </dependency>

    <dependency>
      <groupId>io.temporal</groupId>
      <artifactId>temporal-sdk</artifactId>
    </dependency>
    <dependency>
      <groupId>org.postgresql</groupId>
      <artifactId>postgresql</artifactId>
    </dependency>

    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>io.temporal</groupId>
      <artifactId>temporal-testing</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
        <executions>
          <execution>
            <goals>
              <goal>repackage</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
</project>
