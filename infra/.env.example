# Copy to infra/.env. Both optional.
# Overrides agents.gateway in pdlc.yaml; used verbatim, include /v1 if the server expects it.
PDLC_LLM_BASE_URL=
# Leave blank for an endpoint that does not check authentication.
PDLC_LLM_API_KEY=
