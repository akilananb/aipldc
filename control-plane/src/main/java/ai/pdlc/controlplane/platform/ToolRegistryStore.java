package ai.pdlc.controlplane.platform;

/** The ToolDefinition registry's store ({@code tool_definitions} / {@code tool_definition_versions}). */
public interface ToolRegistryStore extends DefinitionStore {
}
